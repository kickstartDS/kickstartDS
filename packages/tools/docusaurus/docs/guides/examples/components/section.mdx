---
sidebar_position: 4
sidebar_label: Extend Section component
---

# Extend `Section` component

TODO describe workflow this stands for (adapt vs adopt)

## Overview

## Requirements

## 1. Component Definition

### Purpose

### Structure

| Property      | Type       | Description                       |
| ------------- | ---------- | --------------------------------- |
| `headline`    | `string`   | Headline for the section          |
| `width`       | `enum`     | Width of section                  |
| `gutter`      | `enum`     | Gutter of section                 |
| `mode`        | `enum`     | Layout mode for section           |
| `content`     | `children` | Allowed content for the section   |
| `style`       | `enum`     | Style variation of section        |
| `spaceBefore` | `enum`     | (Optional) space before section   |
| `spaceAfter`  | `enum`     | (Optional) space after section    |
| `inverted`    | `boolean`  | Show inverted version of section? |
| `ctas`        | `object`   | Optional targets to link          |

TODO should probably make sub fields of `ctas` explicit in table

## 2. Component Creation

### **JSON Schema** definition

### **React** Template

## Use of **Storybook**

## Completed component

## Technical debt

## Advanced `allOf` / `$ref` variant of JSON Schema

## Open TODOs
