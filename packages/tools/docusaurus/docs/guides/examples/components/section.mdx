---
sidebar_position: 4
sidebar_label: Extend Section component
---

# Extend `Section` component

TODO describe workflow this stands for (adapt vs adopt)

## Overview

## Requirements

## 1. Component Definition

### Purpose

### Structure

| Property      | Description                       |
| ------------- | --------------------------------- |
| `headline`    | Headline for the section          |
| `width`       | Width of section                  |
| `gutter`      | Gutter of section                 |
| `mode`        | Layout mode for section           |
| `content`     | Allowed content for the section   |
| `style`       | Style variation of section        |
| `spaceBefore` | (Optional) space before section   |
| `spaceAfter`  | (Optional) space after section    |
| `inverted`    | Show inverted version of section? |
| `ctas`        | Optional targets to link          |

TODO should probably make sub fields of `ctas` explicit in table

## 2. Component Creation

### **JSON Schema** definition

### **React** Template

## Use of **Storybook**

## Completed component

## Technical debt

## Advanced `allOf` / `$ref` variant of JSON Schema

## Open TODOs
