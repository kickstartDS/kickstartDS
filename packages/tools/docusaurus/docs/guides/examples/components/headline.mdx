---
sidebar_position: 2
sidebar_label: Customize Headline component
---

# Customize `Headline` component

TODO describe workflow this stands for (adapt vs adopt)

## Overview

## Requirements

## 1. Component Definition

### Purpose

### Structure

| Property      | Type      | Description                              |
| ------------- | --------- | ---------------------------------------- |
| `level *`     | `enum`    | Level of headline to use                 |
| `style`       | `enum`    | Style of headline to show                |
| `text *`      | `string`  | Text content of headline                 |
| `sub`         | `string`  | (Optional) Subheadline content           |
| `switchOrder` | `boolean` | Switch order of headline and subheadline |
| `spaceAfter`  | `enum`    | (Optional) spacing after headline        |

## 2. Component Creation

### **JSON Schema** definition

### **React** Template

## Use of **Storybook**

## Completed component

## Technical debt

## Advanced `allOf` / `$ref` variant of JSON Schema

## Open TODOs
