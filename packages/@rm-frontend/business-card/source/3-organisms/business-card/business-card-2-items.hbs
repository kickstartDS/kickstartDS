{{#> base-section background="1" headline=(object content="Business card" subheader="2 column")}}
  <div class="business-card-wrap business-card-wrap--col-2">
    <div class="business-card">
      {{#each (slice business-card 0 2)}}
        <div class="business-card__column">
          {{> molecules-business-card-item this}}
        </div>
      {{/each}}
    </div>
  </div>
{{/base-section}}
