{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "http://schema.kickstartds.com/base/notification.schema.json",
  "title": "Notification",
  "description": "Component to render a pill / tag / label",
  "type": "object",
  "properties": {
    "title": {
      "title": "Title",
      "description": "A short title of the notification",
      "type": "string",
      "examples": ["Updated policy"]
    },
    "message": {
      "title": "Message",
      "description": "The main message delivered by the notification",
      "type": "string",
      "examples": [
        "We have updated our policy regarding shipments. [Check it our here](#)."
      ]
    },
    "type": {
      "title": "Type",
      "description": "Choose the type of information being displayed in the notification",
      "type": "string",
      "enum": ["plain", "info", "warning", "succes", "error", "user"],
      "default": "plain"
    },
    "actions": {
      "type": "object",
      "properties": {
        "close": {
          "title": "Close",
          "description": "Choose whether the element is closable or not",
          "type": "boolean"
        },
        "undo": {
          "title": "Undo",
          "description": "Choose whether the action can be undone or not",
          "type": "boolean"
        },
        "accept": {
          "title": "Reply",
          "description": "Choose whether the action can be undone or not",
          "type": "boolean"
        }
      }
    },
    "className": {
      "title": "Additional Classes",
      "description": "Add additional css classes that should be applied to the label",
      "type": "string"
    },
    "component": {
      "title": "`ks-component` attribute",
      "description": "Optional custom component identifier",
      "type": "string"
    }
  },
  "required": ["label"],
  "additionalProperties": false
}
